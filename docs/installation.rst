============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install flake8-svg-badge
    $ pip install flake8-svg-badge

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flake8-svg-badge
    $ pip install flake8-svg-badge
